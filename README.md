# CarbonFiberglass – Ultimate VM Killer

Bu proje, sanal makine (VM) içinde çalıştırılmak üzere tasarlanmış **görsel ve işitsel bir simülasyondur**. 
Amacı, Monoxide.exe tarzı kaotik efektler oluşturmaktır. **Hiçbir dosyaya, kernele veya sisteme dokunmaz.**

---

## 📁 Dosyalar

| Dosya | Açıklama |
|-------|----------|
| `CarbonFiberglass.bat` | Ana çalıştırıcı |
| `modul_*.ps1` | Görsel, ses ve efekt modülleri |
| `muzik_3.mp3` | Telifsiz arka plan müziği |

---

## ⚠️ Uyarı

- **SADECE SANAL MAKİNE İÇİNDE TEST EDİN.**
- Gerçek sistemde çalıştırmak kalıcı hasara yol açar.
- Antivirüs bypass kodları yalnızca test amaçlıdır.

---

## 🚀 Çalıştırma

1. Tüm dosyaları aynı klasöre koyun.
2. `CarbonFiberglass.bat` dosyasına çift tıklayın.
3. 8-10 dakika sonra BSOD ile VM kapanacaktır.

---

## 🛠️ Modüller

- `modul_isik_patlamasi.ps1` – Işık patlamaları
- `modul_glitch.ps1` – Ekran bozulması
- `modul_renk.ps1` – Renk patlamaları
- `modul_hata.ps1` – Hata yağmuru
- `modul_ses.ps1` – Ses kaosu
- `modul_fare.ps1` – Fare kontrolsüz hareket
- `modul_anormallik.ps1` – Anormallikler
- `modul_sembol.ps1` – Uçuşan semboller
- `modul_disk.ps1` – Disk %100 simülasyonu
- `modul_rom.ps1` – RAM/ROM %100 simülasyonu
- `modul_antivirus_bypass.ps1` – Antivirüs bypass
- `modul_muzik.ps1` – Arka plan müziği
- `modul_efekt_dongusu.ps1` – Her dakika farklı efekt
- `modul_bsod.ps1` – BSOD ile VM'yi kapatır

---

## 📌 Not

Müzik dosyası (`muzik_3.mp3`) telifsiz olup, 30 saniyelik döngü halinde çalacak şekilde ayarlanmıştır.

---

**İyi testler! 🐋**